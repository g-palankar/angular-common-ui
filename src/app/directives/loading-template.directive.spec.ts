import { LoadingTemplateDirective } from './loading-template.directive';

describe('LoadingTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
